{
  "name": "pptx-tools-core",
  "version": "0.1.5",
  "description": "Host-agnostic engine for pptx-sync: pptx parsing, search, sync planner/executor, event-schedule model, and view-string builders. Consumed by the VS Code extension and the PWA.",
  "license": "MIT",
  "private": true,
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p test/tsconfig.json"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "jsonc-parser": "^3.3.1"
  }
}
